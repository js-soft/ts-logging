# this tells npm to use the pre-v7 peer dependency resolution behavior, which skips strict conflict checks — appropriate here since madge hasn't updated its peer dep range to include TypeScript 6 yet.
legacy-peer-deps=true
